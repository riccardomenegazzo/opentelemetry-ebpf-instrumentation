services:
  go-ownership-receiver:
    build:
      context: ../../..
      dockerfile: internal/test/integration/components/grpc_relay/go/Dockerfile
    image: hatest-grpc-go-ownership-receiver
    init: true
    environment:
      GRPC_PORT: "50070"
      HEALTH_PORT: "8097"
      OBSERVE_TRACEPARENT: "1"
    ports:
      - "127.0.0.1:8097:8097"

  go-ownership-receiver-tls:
    build:
      context: ../../..
      dockerfile: internal/test/integration/components/grpc_relay/go/Dockerfile
    image: hatest-grpc-go-ownership-receiver-tls
    init: true
    environment:
      GRPC_PORT: "50071"
      HEALTH_PORT: "8098"
      GRPC_TLS: "1"
      OBSERVE_TRACEPARENT: "1"
    ports:
      - "127.0.0.1:8098:8098"

  go-ownership-client:
    build:
      context: ../../..
      dockerfile: internal/test/integration/components/grpc_relay/go/Dockerfile
    image: hatest-grpc-go-ownership-client
    init: true
    environment:
      HTTP_PORT: "8082"
      OWNERSHIP_NEXT_HOP: "go-ownership-receiver:50070"
    ports:
      - "127.0.0.1:18082:8082"
    depends_on:
      go-ownership-receiver:
        condition: service_started

  go-ownership-client-tls:
    build:
      context: ../../..
      dockerfile: internal/test/integration/components/grpc_relay/go/Dockerfile
    image: hatest-grpc-go-ownership-client-tls
    init: true
    environment:
      HTTP_PORT: "8083"
      OWNERSHIP_NEXT_HOP: "go-ownership-receiver-tls:50071"
      GRPC_NEXT_HOP_TLS: "1"
    ports:
      - "127.0.0.1:18083:8083"
    depends_on:
      go-ownership-receiver-tls:
        condition: service_started

  go-ownership-client-legacy:
    build:
      context: ../../..
      dockerfile: internal/test/integration/components/grpc_relay/go_legacy/Dockerfile
    image: hatest-grpc-go-ownership-client-legacy
    init: true
    environment:
      HTTP_PORT: "8084"
      OWNERSHIP_NEXT_HOP: "go-ownership-receiver:50070"
    ports:
      - "127.0.0.1:18084:8084"
    depends_on:
      go-ownership-receiver:
        condition: service_started

  go-ownership-client-legacy-tls:
    build:
      context: ../../..
      dockerfile: internal/test/integration/components/grpc_relay/go_legacy/Dockerfile
    image: hatest-grpc-go-ownership-client-legacy-tls
    init: true
    environment:
      HTTP_PORT: "8085"
      OWNERSHIP_NEXT_HOP: "go-ownership-receiver-tls:50071"
      GRPC_NEXT_HOP_TLS: "1"
    ports:
      - "127.0.0.1:18085:8085"
    depends_on:
      go-ownership-receiver-tls:
        condition: service_started

  obi:
    build:
      context: ../../..
      dockerfile: ./internal/test/integration/components/obi/Dockerfile
    command:
      - --config=/configs/obi-config-grpc-go-traceparent.yml
    volumes:
      - ./configs/:/configs
      - ./system/sys/kernel/security${SECURITY_CONFIG_SUFFIX}:/sys/kernel/security
      - /sys/fs/bpf:/sys/fs/bpf
      - /sys/fs/cgroup:/sys/fs/cgroup
      - /sys/kernel/tracing:/sys/kernel/tracing:ro
      - ../../../testoutput:/coverage
      - ../../../testoutput/run-grpc-go-traceparent:/var/run/obi
    image: hatest-obi
    privileged: true
    init: true
    pid: "host"
    cgroup: "host"
    environment:
      GOCOVERDIR: "/coverage"
      OTEL_EBPF_TRACE_PRINTER: "text"
      OTEL_EBPF_METRICS_INTERVAL: "1s"
      OTEL_EBPF_BPF_BATCH_TIMEOUT: "250ms"
      OTEL_EBPF_OTLP_TRACES_BATCH_TIMEOUT: "200ms"
      OTEL_EBPF_HOSTNAME: "beyla"
      OTEL_EBPF_BPF_CONTEXT_PROPAGATION: "all"
      OTEL_EBPF_DISCOVERY_POLL_INTERVAL: "1s"
      OTEL_EBPF_LOG_LEVEL: "DEBUG"
    depends_on:
      go-ownership-client:
        condition: service_started
      go-ownership-client-tls:
        condition: service_started
      go-ownership-client-legacy:
        condition: service_started
      go-ownership-client-legacy-tls:
        condition: service_started
      jaeger:
        condition: service_started

  jaeger:
    image: jaegertracing/jaeger:2.21.0@sha256:3d0ac795ff98aa04d1be04311d2dac6c25b4bfc8322dc02e53bc5b170c5018c3
    ports:
      - "16686:16686"
      - "4317:4317"
      - "4318:4318"
    command:
      - "--set=service.telemetry.logs.level=debug"
